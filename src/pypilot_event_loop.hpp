#pragma once

#if defined(PYPILOT_EVENT_LOOP_ENABLE_ARDUINO_WIFI_TCP) && !defined(ASYNC_EVENT_LOOP_ENABLE_ARDUINO_WIFI_TCP)
#define ASYNC_EVENT_LOOP_ENABLE_ARDUINO_WIFI_TCP PYPILOT_EVENT_LOOP_ENABLE_ARDUINO_WIFI_TCP
#endif
#if defined(PYPILOT_EVENT_LOOP_ENABLE_ARDUINO_WIFI_UDP) && !defined(ASYNC_EVENT_LOOP_ENABLE_ARDUINO_WIFI_UDP)
#define ASYNC_EVENT_LOOP_ENABLE_ARDUINO_WIFI_UDP PYPILOT_EVENT_LOOP_ENABLE_ARDUINO_WIFI_UDP
#endif
#if defined(ASYNC_EVENT_LOOP_ENABLE_ARDUINO_WIFI_TCP) && !defined(PYPILOT_EVENT_LOOP_ENABLE_ARDUINO_WIFI_TCP)
#define PYPILOT_EVENT_LOOP_ENABLE_ARDUINO_WIFI_TCP ASYNC_EVENT_LOOP_ENABLE_ARDUINO_WIFI_TCP
#endif
#if defined(ASYNC_EVENT_LOOP_ENABLE_ARDUINO_WIFI_UDP) && !defined(PYPILOT_EVENT_LOOP_ENABLE_ARDUINO_WIFI_UDP)
#define PYPILOT_EVENT_LOOP_ENABLE_ARDUINO_WIFI_UDP ASYNC_EVENT_LOOP_ENABLE_ARDUINO_WIFI_UDP
#endif

#include <async_event_loop.hpp>

namespace pypilot_event_loop = async_event_loop;
